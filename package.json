{
  "name": "cv",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./public && mkdir ./public && resumed render -o ./public/index.html"
  },
  "author": "ratenio",
  "license": "MIT",
  "dependencies": {
    "jsonresume-theme-even": "^0.23.0",
    "resumed": "^6.0.0"
  }
}
